<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.norbert.balazs.movies.presentation.home.HomeFragment"
        android:label="@string/home" />
    <fragment
        android:id="@+id/favoritesFragment"
        android:name="com.norbert.balazs.movies.presentation.favorites.FavoritesFragment"
        android:label="@string/favorites"
        tools:layout="@layout/favorites_fragment" />
    <fragment
        android:id="@+id/searchFragment"
        android:name="com.norbert.balazs.movies.presentation.search.SearchFragment"
        android:label="@string/search"
        tools:layout="@layout/search_fragment" />

</navigation>